# package dfines
APP_NAME = "poilite"
TARGET = "example.rb"
FILES = [
  "example.rb",
  "poilite.rb",
  "jarlib/*"
]

# work directories
WORK_DIR = "build"
DIST_DIR = "dist"

task :default => "jar:package"
load "./rake/packagejar"
